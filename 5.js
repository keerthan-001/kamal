function getStringLength(str) {
    return str.length;
}

// Example usage
const inputString = "KEERTHAN";
console.log(`The length of the string "${inputString}" is: ${getStringLength(inputString)}`);
